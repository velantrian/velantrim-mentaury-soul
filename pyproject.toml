[build-system]
requires = ["setuptools==82.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mentaury-soul"
version = "1.0.0rc1"
description = "Substrate-neutral research architecture for persistent digital individuality and governed cognition"
readme = "README.md"
requires-python = ">=3.13,<3.14"
license = { text = "Research-only; license not yet selected" }
authors = [{ name = "Velantrim" }]
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["mentaury*"]

[tool.setuptools.package-data]
mentaury = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "scripts"]
addopts = "-q --strict-config --strict-markers"

[tool.coverage.run]
branch = true
source = ["mentaury"]

[tool.coverage.report]
show_missing = true
skip_covered = true
